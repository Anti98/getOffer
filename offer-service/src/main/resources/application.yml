spring:
  application:
    name: offer-service

  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}

    consumer:
      group-id: ${OFFER_CONSUMER_GROUP:offer-group}
      key-deserializer: org.springframework.kafka.support.serializer.ErrorHandlingDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.ErrorHandlingDeserializer
      properties:
        spring.deserializer.key.delegate.class: org.apache.kafka.common.serialization.StringDeserializer
        spring.deserializer.value.delegate.class: org.springframework.kafka.support.serializer.JsonDeserializer
        spring.json.trusted.packages: "*"
        spring.json.use.type.headers: false
        spring.json.value.default.type: ru.ashalyapin.offerservice.event.CalculationCompletedEvent

  mongodb:
    uri: ${MONGODB_URI:mongodb://root:example@localhost:27017/offers_db?authSource=admin}

server:
  port: 8082

logging:
  level:
    org.springframework.boot.autoconfigure: ${OFFER_AUTOCONFIG_LOG_LEVEL:INFO}

app:
  kafka:
    topics:
      calculation-completed: ${TOPIC_CALCULATION_COMPLETED:calculation-completed}
